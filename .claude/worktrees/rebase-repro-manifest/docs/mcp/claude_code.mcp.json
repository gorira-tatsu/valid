{
  "mcpServers": {
    "valid-dsl": {
      "command": "${VALID_MCP_BIN}",
      "args": [
        "--model-file",
        "${VALID_MODEL_FILE}"
      ]
    },
    "valid-registry": {
      "command": "${VALID_MCP_BIN}",
      "args": [
        "--registry-binary",
        "${VALID_REGISTRY_BIN}"
      ]
    }
  }
}
