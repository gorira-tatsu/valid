{
  "mcpServers": {
    "valid-dsl": {
      "command": "valid",
      "args": [
        "mcp",
        "--model-file",
        "${VALID_MODEL_FILE}"
      ]
    },
    "valid-registry": {
      "command": "valid",
      "args": [
        "mcp",
        "--manifest-path",
        "${VALID_PROJECT_MANIFEST}"
      ]
    }
  }
}
